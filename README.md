hi this is my new project custom os so far the name is jiros so far it only has two commands help and clear its bootloader with cross compiler for C running in 32 bit protected mode

so far i compile it all in cygwin64 with make all command to run i just use normal cmd from windows and run command: qemu-system-x86_64 -hda ./bin/os.bin
